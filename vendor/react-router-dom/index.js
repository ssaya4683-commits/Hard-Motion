import React from 'react'; export const BrowserRouter=({children})=>children; export const Router=BrowserRouter; export const Routes=({children})=>React.Children.toArray(children).find(c=>c.props.path===location.pathname)||React.Children.toArray(children)[0]; export const Route=({element})=>element; export const NavLink=({to,children,className,onClick})=>React.createElement('a',{href:to,onClick,className:typeof className==='function'?className({isActive:location.pathname===to}):className},children);
